[package]
name = "cc-deck"
version = "0.2.0"
edition = "2021"
description = "Zellij plugin for managing Claude Code sessions (sidebar + picker)"

[[bin]]
name = "cc_deck"
path = "src/main.rs"

[dependencies]
zellij-tile = "0.43"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
