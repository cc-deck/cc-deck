= Native Installation

Install cc-deck on your local machine for use with an existing Zellij setup.

== Prerequisites

* https://zellij.dev/[Zellij] 0.43 or later
* https://go.dev/[Go] 1.22 or later (for building from source)
* https://www.rust-lang.org/[Rust] stable with the `wasm32-wasip1` target (for building the plugin)

== Quick Install

Clone the repository and build everything:

[source,bash]
----
git clone https://github.com/rhuss/cc-deck.git
cd cc-deck
make install
----

`make install` builds the WASM plugin, compiles the Go CLI, and runs `cc-deck plugin install` to set up Zellij layouts and Claude Code hooks.

== What Gets Installed

The installer places files in your Zellij configuration directory:

[cols="1,2"]
|===
| File | Purpose

| `~/.config/zellij/plugins/cc_deck.wasm`
| The sidebar plugin binary.

| `~/.config/zellij/layouts/cc-deck.kdl`
| Default layout (links to the selected variant).

| `~/.config/zellij/layouts/cc-deck-standard.kdl`
| Standard layout with sidebar, tab bar, and status bar.

| `~/.config/zellij/layouts/cc-deck-minimal.kdl`
| Minimal layout with sidebar and compact bar.

| `~/.config/zellij/layouts/cc-deck-clean.kdl`
| Clean layout with sidebar only.

| `~/.claude/settings.json`
| Claude Code hooks for session tracking (merged, not overwritten).
|===

== Install Options

The `cc-deck plugin install` command accepts several flags:

`--force` / `-f`::
Overwrite existing files without prompting.

`--skip-backup`::
Skip the timestamped backup of `settings.json`.

`--layout <variant>`::
Set the default layout variant.
Options: `standard` (default), `minimal`, `clean`.

`--install-zellij`::
Download and install a matching Zellij binary.

== Layout Variants

Choose a layout variant during installation or switch later:

`standard`::
Sidebar plugin, tab bar, and status bar.
Best for users who want full visibility.

`minimal`::
Sidebar plugin and compact bar.
Saves vertical space while keeping tab information.

`clean`::
Sidebar plugin only.
Maximum terminal space, navigate with keyboard shortcuts.

To change your default layout after installation:

[source,bash]
----
cc-deck plugin install --layout minimal --force
----

== Set as Default Layout

Add this line to `~/.config/zellij/config.kdl` so every new Zellij session uses cc-deck:

[source,kdl]
----
default_layout "cc-deck"
----

== Verify Installation

[source,bash]
----
cc-deck plugin status
----

This shows the installed plugin version and layout configuration.

== Next Steps

* xref:first-session.adoc[First Session] to launch cc-deck and explore the sidebar
* xref:using:sidebar.adoc[User Guide] for all sidebar features and keybindings
* xref:using:configuration.adoc[Configuration] for personal layouts and Ghostty integration
