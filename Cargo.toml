[package]
name = "cc-deck"
version = "0.1.0"
edition = "2021"
description = "Zellij plugin for managing multiple Claude Code sessions"

[lib]
crate-type = ["cdylib"]

[dependencies]
zellij-tile = "0.42"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
