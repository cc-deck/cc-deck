# Implementation Plan: Project-Local Environment Configuration

**Branch**: `026-project-local-config` | **Date**: 2026-03-22 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/026-project-local-config/spec.md`

## Summary

Move environment definitions, image build artifacts, and runtime state into a project-scoped `.cc-deck/` directory at the git root. The global state file gains a project registry section so `cc-deck env list` discovers all projects. Implicit name resolution via git-boundary walk eliminates explicit name arguments for in-project commands.

## Technical Context

**Language/Version**: Go 1.25 (from go.mod)
**Primary Dependencies**: cobra v1.10.2 (CLI), gopkg.in/yaml.v3 (YAML), internal/xdg (XDG paths), internal/podman (container interaction), internal/compose (YAML generation)
**Storage**: YAML files: `.cc-deck/environment.yaml` (project-local definition), `.cc-deck/status.yaml` (project-local state), `$XDG_STATE_HOME/cc-deck/state.yaml` (global registry)
**Testing**: `go test ./...` via `make test`; integration tests with temp directories and git repos
**Target Platform**: Linux (primary), macOS (development)
**Project Type**: CLI tool
**Performance Goals**: N/A (CLI startup time already sub-100ms)
**Constraints**: No external dependencies beyond stdlib + existing imports; must use `internal/xdg` package (not `adrg/xdg`)
**Scale/Scope**: Single developer, tens of projects max

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Gate | Status | Notes |
|-----------|------|--------|-------|
| I. Two-Component Architecture | CLI-only feature | PASS | No plugin changes |
| II. Plugin Installation | Not applicable | PASS | No WASM changes |
| VI. Build via Makefile Only | Use `make test/lint` | PASS | |
| VII. Interface Behavioral Contracts | FR-023 references contract | PASS | All env operations satisfy environment-interface.md |
| VIII. Simplicity | Single env per project | PASS | No multi-env, deferred complexity |
| IX. Documentation Freshness | Required at implementation | GATE | README, CLI ref, Antora guide |
| XII. Prose Plugin | Required for docs | GATE | Use cc-deck voice profile |
| XIII. XDG Paths | Global registry in `$XDG_STATE_HOME` | PASS | Uses internal/xdg |
| XIV. No Dotfile Nesting | FR-024 exception documented | PASS | `.gitignore` only |

**Post-design re-check**: No new violations introduced. The `project` package uses no dotfile names. The `.cc-deck/.gitignore` exception remains the only deviation.

## Project Structure

### Documentation (this feature)

```text
specs/026-project-local-config/
├── plan.md              # This file
├── spec.md              # Feature specification (30 FRs, 8 SCs)
├── research.md          # Phase 0 research findings
├── data-model.md        # Phase 1 data model
├── quickstart.md        # Phase 1 implementation guide
├── contracts/           # Phase 1 interface contracts
│   └── project-discovery.md
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Phase 2 task breakdown (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
cc-deck/internal/
├── project/             # NEW: Git root detection, project discovery
│   ├── project.go       #   FindGitRoot, FindProjectConfig, CanonicalPath
│   ├── worktree.go      #   ListWorktrees, WorktreeInfo
│   └── project_test.go  #   Unit tests
├── env/
│   ├── types.go         # MODIFIED: Add ProjectEntry, ProjectStatusFile
│   ├── state.go         # MODIFIED: Add registry methods
│   ├── definition.go    # MODIFIED: Add project-local loader
│   ├── project_status.go # NEW: ProjectStatusStore
│   ├── compose.go       # MODIFIED: Artifact paths, delete behavior
│   └── container.go     # MODIFIED: Project-local awareness
├── compose/
│   └── generate.go      # MODIFIED: Proxy volume paths
├── build/
│   └── init.go          # MODIFIED: Default to .cc-deck/image/
├── cmd/
│   ├── env.go           # MODIFIED: init/prune commands, optional name, variant, worktrees
│   └── build.go         # MODIFIED: Default dir resolution
└── ...
```

**Structure Decision**: All new code goes into existing `cc-deck/internal/` hierarchy. One new package (`project/`) for git operations. No new top-level directories.

## Complexity Tracking

No constitution violations to justify. All design decisions align with existing principles.
